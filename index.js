require('dotenv').config();

const path = require('node:path');
const {
  Client,
  Collection,
  GatewayIntentBits,
  REST,
  Routes
} = require('discord.js');

const __moduleSources = {"src/commands/config-vendas.js": "const {\n  SlashCommandBuilder,\n  PermissionFlagsBits,\n  ChannelType\n} = require('discord.js');\nconst db = require('../database');\nconst { BUTTON_STYLE_CHOICES } = require('../constants');\nconst { parseHexColor, formatBRLFromCents } = require('../utils/format');\nconst { buildSalesPanel } = require('../utils/embeds');\n\nmodule.exports = {\n  data: new SlashCommandBuilder()\n    .setName('config-vendas')\n    .setDescription('Edita o painel e as configurações do bot de vendas.')\n    .setDefaultMemberPermissions(PermissionFlagsBits.Administrator)\n    .addSubcommand((sub) =>\n      sub\n        .setName('painel')\n        .setDescription('Edita título, descrição, cor e imagem do painel.')\n        .addStringOption((opt) => opt.setName('titulo').setDescription('Novo título do painel.').setMaxLength(256))\n        .addStringOption((opt) => opt.setName('descricao').setDescription('Nova descrição do painel.').setMaxLength(4000))\n        .addStringOption((opt) => opt.setName('cor').setDescription('Cor HEX do embed. Exemplo: #00ff88').setMaxLength(7))\n        .addStringOption((opt) => opt.setName('imagem').setDescription('URL da imagem/banner do painel.'))\n    )\n    .addSubcommand((sub) =>\n      sub\n        .setName('botao')\n        .setDescription('Edita texto, emoji e cor dos botões do painel.')\n        .addStringOption((opt) =>\n          opt\n            .setName('botao')\n            .setDescription('Qual botão editar?')\n            .setRequired(true)\n            .addChoices(\n              { name: 'Comprar salas', value: 'buy' },\n              { name: 'Ver saldo', value: 'balance' },\n              { name: 'Criar sala', value: 'create' }\n            )\n        )\n        .addStringOption((opt) => opt.setName('texto').setDescription('Novo texto do botão.').setMaxLength(80))\n        .addStringOption((opt) => opt.setName('emoji').setDescription('Novo emoji do botão. Exemplo: 🛒'))\n        .addStringOption((opt) =>\n          opt\n            .setName('cor')\n            .setDescription('Cor do botão no Discord.')\n            .addChoices(...BUTTON_STYLE_CHOICES)\n        )\n    )\n    .addSubcommand((sub) =>\n      sub\n        .setName('canais')\n        .setDescription('Configura cargo dono, categoria dos pedidos e canal de logs.')\n        .addRoleOption((opt) => opt.setName('cargo_dono').setDescription('Cargo que pode confirmar pagamentos.'))\n        .addChannelOption((opt) =>\n          opt\n            .setName('categoria_pedidos')\n            .setDescription('Categoria onde os canais privados dos pedidos serão criados.')\n            .addChannelTypes(ChannelType.GuildCategory)\n        )\n        .addChannelOption((opt) =>\n          opt\n            .setName('canal_logs')\n            .setDescription('Canal onde o bot manda logs de pedidos e salas.')\n            .addChannelTypes(ChannelType.GuildText)\n        )\n    )\n    .addSubcommand((sub) =>\n      sub\n        .setName('preco')\n        .setDescription('Configura o preço por sala em centavos.')\n        .addIntegerOption((opt) =>\n          opt\n            .setName('centavos')\n            .setDescription('Preço de cada sala em centavos. Exemplo: 4 = R$0,04')\n            .setMinValue(1)\n            .setMaxValue(100000)\n            .setRequired(true)\n        )\n    )\n    .addSubcommand((sub) => sub.setName('ver').setDescription('Mostra as configurações atuais.')),\n\n  async execute(interaction) {\n    const guildId = interaction.guildId;\n    const sub = interaction.options.getSubcommand();\n\n    if (sub === 'painel') {\n      const title = interaction.options.getString('titulo');\n      const description = interaction.options.getString('descricao');\n      const colorInput = interaction.options.getString('cor');\n      const image = interaction.options.getString('imagem');\n\n      if (colorInput && !parseHexColor(colorInput)) {\n        return interaction.reply({ content: '❌ Cor inválida. Use HEX tipo `#00ff88`.', ephemeral: true });\n      }\n\n      const settings = db.updateSettings(guildId, (current) => {\n        if (title !== null) current.panel.title = title;\n        if (description !== null) current.panel.description = description.replace(/\\\\n/g, '\\n');\n        if (colorInput !== null) current.panel.color = parseHexColor(colorInput);\n        if (image !== null) current.panel.imageUrl = image;\n      });\n\n      return interaction.reply({\n        content: '✅ Painel atualizado. Prévia:',\n        ...buildSalesPanel(settings),\n        ephemeral: true\n      });\n    }\n\n    if (sub === 'botao') {\n      const button = interaction.options.getString('botao', true);\n      const label = interaction.options.getString('texto');\n      const emoji = interaction.options.getString('emoji');\n      const style = interaction.options.getString('cor');\n\n      const settings = db.updateSettings(guildId, (current) => {\n        if (label !== null) current.buttons[button].label = label;\n        if (emoji !== null) current.buttons[button].emoji = emoji;\n        if (style !== null) current.buttons[button].style = style;\n      });\n\n      return interaction.reply({\n        content: '✅ Botão atualizado. Prévia:',\n        ...buildSalesPanel(settings),\n        ephemeral: true\n      });\n    }\n\n    if (sub === 'canais') {\n      const role = interaction.options.getRole('cargo_dono');\n      const category = interaction.options.getChannel('categoria_pedidos');\n      const logs = interaction.options.getChannel('canal_logs');\n\n      const settings = db.updateSettings(guildId, (current) => {\n        if (role) current.ownerRoleId = role.id;\n        if (category) current.ordersCategoryId = category.id;\n        if (logs) current.logsChannelId = logs.id;\n      });\n\n      return interaction.reply({\n        content: [\n          '✅ Configurações de canais/cargos atualizadas.',\n          `Cargo dono/staff: ${settings.ownerRoleId ? `<@&${settings.ownerRoleId}>` : '`não definido`'}`,\n          `Categoria dos pedidos: ${settings.ordersCategoryId ? `<#${settings.ordersCategoryId}>` : '`não definida`'}`,\n          `Canal de logs: ${settings.logsChannelId ? `<#${settings.logsChannelId}>` : '`não definido`'}`\n        ].join('\\n'),\n        ephemeral: true\n      });\n    }\n\n    if (sub === 'preco') {\n      const cents = interaction.options.getInteger('centavos', true);\n      db.updateSettings(guildId, (current) => {\n        current.priceCentsPerRoom = cents;\n      });\n      return interaction.reply({\n        content: `✅ Preço por sala atualizado para **${formatBRLFromCents(cents)}**.`,\n        ephemeral: true\n      });\n    }\n\n    if (sub === 'ver') {\n      const settings = db.getSettings(guildId);\n      return interaction.reply({\n        content: [\n          '**Configurações atuais:**',\n          `Título: ${settings.panel.title}`,\n          `Cor: ${settings.panel.color}`,\n          `Imagem: ${settings.panel.imageUrl || '`sem imagem`'}`,\n          `Preço por sala: **${formatBRLFromCents(settings.priceCentsPerRoom)}**`,\n          `Cargo dono/staff: ${settings.ownerRoleId ? `<@&${settings.ownerRoleId}>` : '`não definido`'}`,\n          `Categoria dos pedidos: ${settings.ordersCategoryId ? `<#${settings.ordersCategoryId}>` : '`não definida`'}`,\n          `Canal de logs: ${settings.logsChannelId ? `<#${settings.logsChannelId}>` : '`não definido`'}`,\n          '',\n          '**Prévia do painel:**'\n        ].join('\\n'),\n        ...buildSalesPanel(settings),\n        ephemeral: true\n      });\n    }\n  }\n};\n", "src/commands/criar-sala.js": "const { SlashCommandBuilder } = require('discord.js');\nconst { ROOM_MODE_OPTIONS, getRoomModeOption } = require('../constants');\nconst { createRoomForInteraction } = require('../handlers/roomCreate');\n\nmodule.exports = {\n  data: new SlashCommandBuilder()\n    .setName('criar-sala')\n    .setDescription('Cria uma sala usando 1 saldo da sua conta.')\n    .addIntegerOption((opt) =>\n      opt\n        .setName('modo')\n        .setDescription('Modo da sala.')\n        .setRequired(true)\n        .addChoices(...ROOM_MODE_OPTIONS.map((mode) => ({ name: `${mode.value} - ${mode.name}`, value: Number(mode.value) })))\n    )\n    .addStringOption((opt) => opt.setName('nome').setDescription('Nome da sala. Se não colocar, usa um nome padrão.').setMaxLength(30))\n    .addStringOption((opt) => opt.setName('senha').setDescription('Senha da sala. Se não colocar, usa sua senha fixa do /perfil.').setMaxLength(12))\n    .addIntegerOption((opt) =>\n      opt\n        .setName('tempo_go')\n        .setDescription('Tempo de GO/início automático em minutos. Se não colocar, usa seu /perfil.')\n        .setMinValue(1)\n        .setMaxValue(8)\n    ),\n\n  async execute(interaction) {\n    await interaction.deferReply({ ephemeral: true });\n\n    const modeOption = interaction.options.getInteger('modo', true);\n    const selected = getRoomModeOption(modeOption);\n\n    if (!selected) {\n      return interaction.editReply('❌ Modo inválido. Use um modo de 1 a 5.');\n    }\n\n    const payload = {\n      modo: selected.apiMode,\n      modeOption: selected.value,\n      nome: interaction.options.getString('nome') || `Sala ${interaction.user.username}`,\n      senha: interaction.options.getString('senha') || '',\n      tempoGo: interaction.options.getInteger('tempo_go') || null\n    };\n\n    return createRoomForInteraction(interaction, payload, { alreadyDeferred: true });\n  }\n};\n", "src/commands/painel-vendas.js": "const {\n  SlashCommandBuilder,\n  PermissionFlagsBits,\n  ChannelType\n} = require('discord.js');\nconst db = require('../database');\nconst { buildSalesPanel } = require('../utils/embeds');\n\nmodule.exports = {\n  data: new SlashCommandBuilder()\n    .setName('painel-vendas')\n    .setDescription('Envia o painel de vendas no canal escolhido.')\n    .setDefaultMemberPermissions(PermissionFlagsBits.Administrator)\n    .addChannelOption((opt) =>\n      opt\n        .setName('canal')\n        .setDescription('Canal onde o painel será enviado. Se não escolher, usa o canal atual.')\n        .addChannelTypes(ChannelType.GuildText)\n    ),\n\n  async execute(interaction) {\n    const channel = interaction.options.getChannel('canal') || interaction.channel;\n    const settings = db.getSettings(interaction.guildId);\n\n    await channel.send(buildSalesPanel(settings));\n    return interaction.reply({ content: `✅ Painel enviado em ${channel}.`, ephemeral: true });\n  }\n};\n", "src/commands/perfil.js": "const { SlashCommandBuilder } = require('discord.js');\nconst db = require('../database');\nconst { buildProfileEmbed, buildProfileButtons } = require('../utils/embeds');\n\nmodule.exports = {\n  data: new SlashCommandBuilder()\n    .setName('perfil')\n    .setDescription('Configura sua senha fixa, tempo de GO e prefixo das suas salas.'),\n\n  async execute(interaction) {\n    const userData = db.getUser(interaction.guildId, interaction.user.id);\n    return interaction.reply({\n      embeds: [buildProfileEmbed(interaction.user, userData)],\n      components: buildProfileButtons(interaction.user.id),\n      ephemeral: true\n    });\n  }\n};\n", "src/commands/saldo.js": "const { SlashCommandBuilder } = require('discord.js');\nconst db = require('../database');\nconst { buildBalanceEmbed } = require('../utils/embeds');\nconst { isOwnerStaff } = require('../utils/permissions');\n\nmodule.exports = {\n  data: new SlashCommandBuilder()\n    .setName('saldo')\n    .setDescription('Mostra seu saldo de salas.')\n    .addUserOption((opt) => opt.setName('usuario').setDescription('Ver saldo de outro usuário. Apenas dono/staff/admin.')),\n\n  async execute(interaction) {\n    const settings = db.getSettings(interaction.guildId);\n    const target = interaction.options.getUser('usuario') || interaction.user;\n\n    if (target.id !== interaction.user.id && !isOwnerStaff(interaction.member, settings)) {\n      return interaction.reply({ content: '❌ Você só pode ver o próprio saldo.', ephemeral: true });\n    }\n\n    const data = db.getUser(interaction.guildId, target.id);\n    const memberText = target.id === interaction.user.id ? interaction.user : `<@${target.id}>`;\n    return interaction.reply({ embeds: [buildBalanceEmbed(memberText, data)], ephemeral: true });\n  }\n};\n", "src/constants.js": "const { ButtonStyle } = require('discord.js');\n\nconst BUTTON_STYLES = {\n  Primary: ButtonStyle.Primary,\n  Secondary: ButtonStyle.Secondary,\n  Success: ButtonStyle.Success,\n  Danger: ButtonStyle.Danger\n};\n\nconst BUTTON_STYLE_CHOICES = [\n  { name: 'Azul', value: 'Primary' },\n  { name: 'Cinza', value: 'Secondary' },\n  { name: 'Verde', value: 'Success' },\n  { name: 'Vermelho', value: 'Danger' }\n];\n\nconst ROOM_MODES = {\n  1: '4x4 Padrão Apostado',\n  2: '4x4 5 Gelo',\n  3: 'Gel Infinito',\n  4: 'Capa Nv3',\n  5: 'Full Capa'\n};\n\nconst ROOM_MODE_OPTIONS = [\n  {\n    value: '1',\n    name: '4x4 Padrão Apostado',\n    description: 'Modo 4x4 padrão apostado',\n    emoji: '🎮',\n    apiMode: 2\n  },\n  {\n    value: '2',\n    name: '4x4 5 Gelo',\n    description: 'Modo 4x4 com 5 gelo',\n    emoji: '✅',\n    apiMode: 2\n  },\n  {\n    value: '3',\n    name: 'Gel Infinito',\n    description: 'Modo gel infinito',\n    emoji: '⚡',\n    apiMode: 3\n  },\n  {\n    value: '4',\n    name: 'Capa Nv3',\n    description: 'Modo feito para revanche',\n    emoji: '✅',\n    apiMode: 5\n  },\n  {\n    value: '5',\n    name: 'Full Capa',\n    description: 'Modo full capa',\n    emoji: '🔥',\n    apiMode: 4\n  }\n];\n\nfunction getRoomModeOption(value) {\n  return ROOM_MODE_OPTIONS.find((mode) => mode.value === String(value));\n}\n\nconst DEFAULT_SETTINGS = {\n  panel: {\n    title: '🛒 Loja de Salas Free Fire',\n    description: 'Clique em **Comprar salas** para adicionar saldo. Cada sala custa **R$0,04**. Depois use **/criar-sala** ou seu prefixo do /perfil para criar sua sala automaticamente.',\n    color: '#2b2d31',\n    imageUrl: '',\n    footer: 'Sistema de vendas por saldo'\n  },\n  buttons: {\n    buy: { label: 'Comprar salas', emoji: '🛒', style: 'Success' },\n    balance: { label: 'Ver saldo', emoji: '💰', style: 'Primary' },\n    create: { label: 'Criar sala', emoji: '🎮', style: 'Secondary' }\n  },\n  ownerRoleId: null,\n  ordersCategoryId: null,\n  logsChannelId: null,\n  priceCentsPerRoom: 4\n};\n\nmodule.exports = {\n  BUTTON_STYLES,\n  BUTTON_STYLE_CHOICES,\n  ROOM_MODES,\n  ROOM_MODE_OPTIONS,\n  getRoomModeOption,\n  DEFAULT_SETTINGS\n};\n", "src/database.js": "const fs = require('node:fs');\nconst path = require('node:path');\nconst { DEFAULT_SETTINGS } = require('./constants');\n\nconst dataDir = path.join(process.cwd(), 'data');\nconst dbPath = path.join(dataDir, 'database.json');\n\nfunction clone(value) {\n  return JSON.parse(JSON.stringify(value));\n}\n\nfunction ensureDb() {\n  if (!fs.existsSync(dataDir)) fs.mkdirSync(dataDir, { recursive: true });\n  if (!fs.existsSync(dbPath)) {\n    fs.writeFileSync(dbPath, JSON.stringify({ guilds: {} }, null, 2));\n  }\n}\n\nfunction readDb() {\n  ensureDb();\n  try {\n    const raw = fs.readFileSync(dbPath, 'utf8');\n    const parsed = JSON.parse(raw);\n    if (!parsed.guilds) parsed.guilds = {};\n    return parsed;\n  } catch (error) {\n    console.error('Erro lendo database.json:', error);\n    return { guilds: {} };\n  }\n}\n\nfunction writeDb(db) {\n  ensureDb();\n  const tempPath = `${dbPath}.tmp`;\n  fs.writeFileSync(tempPath, JSON.stringify(db, null, 2));\n  fs.renameSync(tempPath, dbPath);\n}\n\nfunction ensureGuild(db, guildId) {\n  if (!db.guilds[guildId]) {\n    db.guilds[guildId] = {\n      settings: clone(DEFAULT_SETTINGS),\n      users: {},\n      orders: {},\n      rooms: []\n    };\n  }\n\n  const guild = db.guilds[guildId];\n  guild.settings = {\n    ...clone(DEFAULT_SETTINGS),\n    ...(guild.settings || {}),\n    panel: { ...clone(DEFAULT_SETTINGS.panel), ...(guild.settings?.panel || {}) },\n    buttons: {\n      buy: { ...clone(DEFAULT_SETTINGS.buttons.buy), ...(guild.settings?.buttons?.buy || {}) },\n      balance: { ...clone(DEFAULT_SETTINGS.buttons.balance), ...(guild.settings?.buttons?.balance || {}) },\n      create: { ...clone(DEFAULT_SETTINGS.buttons.create), ...(guild.settings?.buttons?.create || {}) }\n    }\n  };\n  if (!guild.users) guild.users = {};\n  if (!guild.orders) guild.orders = {};\n  if (!Array.isArray(guild.rooms)) guild.rooms = [];\n  return guild;\n}\n\nfunction ensureUser(guild, userId) {\n  if (!guild.users[userId]) {\n    guild.users[userId] = {\n      balance: 0,\n      profile: {\n        fixedPassword: '',\n        goTime: null,\n        commandPrefix: '.'\n      },\n      stats: {\n        totalBought: 0,\n        roomsCreated: 0\n      }\n    };\n  }\n\n  const user = guild.users[userId];\n  if (!user.profile) user.profile = { fixedPassword: '', goTime: null, commandPrefix: '.' };\n  if (!user.profile.commandPrefix) user.profile.commandPrefix = '.';\n  if (!user.stats) user.stats = { totalBought: 0, roomsCreated: 0 };\n  if (typeof user.balance !== 'number') user.balance = Number(user.balance || 0);\n  return user;\n}\n\nfunction getGuildData(guildId) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  writeDb(db);\n  return clone(guild);\n}\n\nfunction getSettings(guildId) {\n  return getGuildData(guildId).settings;\n}\n\nfunction updateSettings(guildId, updater) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  updater(guild.settings);\n  writeDb(db);\n  return clone(guild.settings);\n}\n\nfunction getUser(guildId, userId) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  const user = ensureUser(guild, userId);\n  writeDb(db);\n  return clone(user);\n}\n\nfunction addBalance(guildId, userId, amount, options = {}) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  const user = ensureUser(guild, userId);\n  user.balance += Number(amount);\n  if (options.countAsPurchase) user.stats.totalBought += Number(amount);\n  writeDb(db);\n  return clone(user);\n}\n\nfunction removeBalance(guildId, userId, amount) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  const user = ensureUser(guild, userId);\n  if (user.balance < amount) return { ok: false, user: clone(user) };\n  user.balance -= Number(amount);\n  writeDb(db);\n  return { ok: true, user: clone(user) };\n}\n\nfunction updateProfile(guildId, userId, data) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  const user = ensureUser(guild, userId);\n  user.profile = { ...user.profile, ...data };\n  writeDb(db);\n  return clone(user.profile);\n}\n\nfunction createOrder(guildId, order) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  guild.orders[order.id] = {\n    ...order,\n    status: 'pending',\n    createdAt: new Date().toISOString(),\n    updatedAt: new Date().toISOString()\n  };\n  writeDb(db);\n  return clone(guild.orders[order.id]);\n}\n\nfunction updateOrder(guildId, orderId, data) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  if (!guild.orders[orderId]) return null;\n  guild.orders[orderId] = {\n    ...guild.orders[orderId],\n    ...data,\n    updatedAt: new Date().toISOString()\n  };\n  writeDb(db);\n  return clone(guild.orders[orderId]);\n}\n\nfunction getOrder(guildId, orderId) {\n  const guild = getGuildData(guildId);\n  return guild.orders[orderId] ? clone(guild.orders[orderId]) : null;\n}\n\nfunction getOrderByChannel(guildId, channelId) {\n  const guild = getGuildData(guildId);\n  return Object.values(guild.orders).find((order) => order.channelId === channelId) || null;\n}\n\nfunction addRoomLog(guildId, userId, room) {\n  const db = readDb();\n  const guild = ensureGuild(db, guildId);\n  const user = ensureUser(guild, userId);\n  user.stats.roomsCreated += 1;\n  guild.rooms.unshift({\n    userId,\n    ...room,\n    createdAt: new Date().toISOString()\n  });\n  guild.rooms = guild.rooms.slice(0, 500);\n  writeDb(db);\n  return clone(user);\n}\n\nmodule.exports = {\n  getGuildData,\n  getSettings,\n  updateSettings,\n  getUser,\n  addBalance,\n  removeBalance,\n  updateProfile,\n  createOrder,\n  updateOrder,\n  getOrder,\n  getOrderByChannel,\n  addRoomLog\n};\n", "src/handlers/interactionCreate.js": "const crypto = require('node:crypto');\nconst {\n  ActionRowBuilder,\n  ChannelType,\n  EmbedBuilder,\n  ModalBuilder,\n  PermissionFlagsBits,\n  TextInputBuilder,\n  TextInputStyle\n} = require('discord.js');\nconst db = require('../database');\nconst salasApi = require('../services/salasApi');\nconst {\n  buildBalanceEmbed,\n  buildOrderButtons,\n  buildOrderEmbed,\n  buildProfileButtons,\n  buildProfileEmbed,\n  buildRoomCreatedEmbed,\n  buildRoomInfoButtons,\n  buildRoomModePanel\n} = require('../utils/embeds');\nconst { cleanChannelName, formatBRLFromCents, onlyNumbers } = require('../utils/format');\nconst { isOwnerStaff } = require('../utils/permissions');\nconst { createRoomForInteraction } = require('./roomCreate');\nconst { getRoomModeOption, ROOM_MODES } = require('../constants');\n\nfunction randomOrderId() {\n  return crypto.randomBytes(4).toString('hex').toUpperCase();\n}\n\nasync function sendLog(guild, settings, payload) {\n  if (!settings.logsChannelId) return;\n  const channel = await guild.channels.fetch(settings.logsChannelId).catch(() => null);\n  if (channel?.isTextBased()) await channel.send(payload).catch(() => null);\n}\n\nasync function handleCommand(interaction) {\n  const command = interaction.client.commands.get(interaction.commandName);\n  if (!command) return;\n\n  try {\n    await command.execute(interaction);\n  } catch (error) {\n    console.error(`Erro no comando /${interaction.commandName}:`, error);\n    const payload = { content: `❌ Erro no comando: ${error.message}`, ephemeral: true };\n    if (interaction.replied || interaction.deferred) await interaction.editReply(payload).catch(() => null);\n    else await interaction.reply(payload).catch(() => null);\n  }\n}\n\nasync function openBuyModal(interaction) {\n  const modal = new ModalBuilder()\n    .setCustomId('order:create')\n    .setTitle('Comprar salas');\n\n  const quantity = new TextInputBuilder()\n    .setCustomId('quantity')\n    .setLabel('Quantidade de salas')\n    .setPlaceholder('Exemplo: 10')\n    .setStyle(TextInputStyle.Short)\n    .setMinLength(1)\n    .setMaxLength(6)\n    .setRequired(true);\n\n  modal.addComponents(new ActionRowBuilder().addComponents(quantity));\n  return interaction.showModal(modal);\n}\n\nasync function handleCreateOrder(interaction) {\n  await interaction.deferReply({ ephemeral: true });\n\n  const settings = db.getSettings(interaction.guildId);\n  const quantityRaw = interaction.fields.getTextInputValue('quantity');\n  const quantity = Number(onlyNumbers(quantityRaw));\n\n  if (!Number.isInteger(quantity) || quantity <= 0) {\n    return interaction.editReply('❌ Coloque uma quantidade válida. Exemplo: `10`.');\n  }\n\n  if (quantity > 100000) {\n    return interaction.editReply('❌ Quantidade muito alta. Tente uma quantidade menor.');\n  }\n\n  const totalCents = quantity * settings.priceCentsPerRoom;\n  const orderId = randomOrderId();\n\n  const overwrites = [\n    {\n      id: interaction.guild.roles.everyone.id,\n      deny: [PermissionFlagsBits.ViewChannel]\n    },\n    {\n      id: interaction.user.id,\n      allow: [\n        PermissionFlagsBits.ViewChannel,\n        PermissionFlagsBits.SendMessages,\n        PermissionFlagsBits.ReadMessageHistory,\n        PermissionFlagsBits.AttachFiles\n      ]\n    },\n    {\n      id: interaction.client.user.id,\n      allow: [\n        PermissionFlagsBits.ViewChannel,\n        PermissionFlagsBits.SendMessages,\n        PermissionFlagsBits.ReadMessageHistory,\n        PermissionFlagsBits.ManageChannels\n      ]\n    }\n  ];\n\n  if (settings.ownerRoleId) {\n    overwrites.push({\n      id: settings.ownerRoleId,\n      allow: [\n        PermissionFlagsBits.ViewChannel,\n        PermissionFlagsBits.SendMessages,\n        PermissionFlagsBits.ReadMessageHistory,\n        PermissionFlagsBits.ManageChannels\n      ]\n    });\n  }\n\n  const channelName = `pedido-${cleanChannelName(interaction.user.username)}-${orderId.toLowerCase()}`;\n  let channel;\n\n  try {\n    channel = await interaction.guild.channels.create({\n      name: channelName,\n      type: ChannelType.GuildText,\n      parent: settings.ordersCategoryId || undefined,\n      permissionOverwrites: overwrites,\n      reason: `Pedido de salas ${orderId} criado por ${interaction.user.tag}`\n    });\n  } catch (error) {\n    console.error('Erro criando canal de pedido:', error);\n    return interaction.editReply('❌ Não consegui criar o canal privado. Veja se eu tenho permissão de **Gerenciar Canais**.');\n  }\n\n  const order = db.createOrder(interaction.guildId, {\n    id: orderId,\n    guildId: interaction.guildId,\n    userId: interaction.user.id,\n    channelId: channel.id,\n    quantity,\n    totalCents,\n    priceCentsPerRoom: settings.priceCentsPerRoom\n  });\n\n  await channel.send({\n    content: `<@${interaction.user.id}> ${settings.ownerRoleId ? `<@&${settings.ownerRoleId}>` : ''}`,\n    embeds: [buildOrderEmbed(order, interaction.user)],\n    components: buildOrderButtons(order.id)\n  });\n\n  await sendLog(interaction.guild, settings, {\n    embeds: [\n      new EmbedBuilder()\n        .setTitle('🛒 Novo pedido criado')\n        .setColor('#f1c40f')\n        .setDescription([\n          `Cliente: <@${interaction.user.id}>`,\n          `Pedido: **${order.id}**`,\n          `Quantidade: **${quantity} sala(s)**`,\n          `Valor: **${formatBRLFromCents(totalCents)}**`,\n          `Canal: ${channel}`\n        ].join('\\n'))\n        .setTimestamp()\n    ]\n  });\n\n  return interaction.editReply(`✅ Pedido criado em ${channel}. Valor: **${formatBRLFromCents(totalCents)}**.`);\n}\n\nasync function handleSalesBalance(interaction) {\n  const userData = db.getUser(interaction.guildId, interaction.user.id);\n  return interaction.reply({ embeds: [buildBalanceEmbed(interaction.user, userData)], ephemeral: true });\n}\n\nasync function openCreateRoomModal(interaction) {\n  const userData = db.getUser(interaction.guildId, interaction.user.id);\n  const prefix = userData.profile?.commandPrefix || '.';\n  return interaction.reply({ ...buildRoomModePanel(interaction.user, prefix, true), ephemeral: true });\n}\n\nasync function handleCreateRoomModal(interaction) {\n  await interaction.deferReply({ ephemeral: true });\n\n  const modo = Number(onlyNumbers(interaction.fields.getTextInputValue('modo')));\n  const nome = interaction.fields.getTextInputValue('nome') || `Sala ${interaction.user.username}`;\n  const senha = interaction.fields.getTextInputValue('senha') || '';\n  const tempoRaw = interaction.fields.getTextInputValue('tempo_go') || '';\n  const tempoGo = tempoRaw ? Number(onlyNumbers(tempoRaw)) : null;\n\n  return createRoomForInteraction(interaction, { modo, nome, senha, tempoGo }, { alreadyDeferred: true });\n}\n\nasync function handleRoomModeSelect(interaction, userId) {\n  if (interaction.user.id !== userId) {\n    return interaction.reply({ content: '❌ Esse menu não é seu.', ephemeral: true });\n  }\n\n  const selected = getRoomModeOption(interaction.values[0]);\n  if (!selected) {\n    return interaction.reply({ content: '❌ Modo inválido.', ephemeral: true });\n  }\n\n  await interaction.deferReply();\n  return createRoomForInteraction(interaction, {\n    modo: selected.apiMode,\n    modeOption: selected.value,\n    nome: `Sala ${interaction.user.username}`\n  }, { alreadyDeferred: true, ephemeral: false });\n}\n\nasync function handleRoomInfoRefresh(interaction, userId, sshash) {\n  if (interaction.user.id !== userId && !isOwnerStaff(interaction.member, db.getSettings(interaction.guildId))) {\n    return interaction.reply({ content: '❌ Só o dono da sala ou staff pode atualizar essa sala.', ephemeral: true });\n  }\n\n  await interaction.deferUpdate();\n\n  try {\n    const info = await salasApi.infoSala(sshash);\n    const oldEmbed = interaction.message.embeds?.[0];\n    const data = {\n      roomId: info?.room?.id || 'Não informado',\n      senha: info?.room?.senha || 'Sem senha',\n      nome: info?.room?.nome || 'Sala',\n      sshash\n    };\n\n    let modeNumber = Number(info?.room?.modo || 0);\n    if (!ROOM_MODES[modeNumber]) modeNumber = 2;\n\n    const embed = buildRoomCreatedEmbed(interaction.user, data, 'não alterado', {\n      modo: modeNumber,\n      modeLabel: ROOM_MODES[modeNumber],\n      senha: data.senha,\n      nome: data.nome,\n      autoStart: null\n    }, info);\n\n    if (oldEmbed?.description) {\n      const saldoMatch = oldEmbed.description.match(/💰 Saldo restante: \\*\\*(.*?)\\*\\*/);\n      if (saldoMatch) {\n        const desc = embed.data.description.replace('💰 Saldo restante: **não alterado sala(s)**', `💰 Saldo restante: **${saldoMatch[1]}**`);\n        embed.setDescription(desc);\n      }\n    }\n\n    return interaction.message.edit({ embeds: [embed], components: buildRoomInfoButtons(sshash, userId) });\n  } catch (error) {\n    console.error('Erro atualizando jogadores:', error);\n    return interaction.followUp({ content: `❌ Não consegui atualizar os jogadores: ${error.message}`, ephemeral: true });\n  }\n}\n\nasync function handleProfileButton(interaction, action, userId) {\n  if (interaction.user.id !== userId) {\n    return interaction.reply({ content: '❌ Esse perfil não é seu.', ephemeral: true });\n  }\n\n  if (action === 'password') {\n    const modal = new ModalBuilder()\n      .setCustomId(`profile:set_password:${userId}`)\n      .setTitle('Editar senha fixa');\n\n    const input = new TextInputBuilder()\n      .setCustomId('password')\n      .setLabel('Senha fixa da sua sala')\n      .setPlaceholder('Exemplo: 33')\n      .setStyle(TextInputStyle.Short)\n      .setRequired(true)\n      .setMaxLength(12);\n\n    modal.addComponents(new ActionRowBuilder().addComponents(input));\n    return interaction.showModal(modal);\n  }\n\n  if (action === 'go') {\n    const modal = new ModalBuilder()\n      .setCustomId(`profile:set_go:${userId}`)\n      .setTitle('Editar tempo de GO');\n\n    const input = new TextInputBuilder()\n      .setCustomId('go')\n      .setLabel('Tempo de GO em minutos, 1 a 8')\n      .setPlaceholder('Exemplo: 5')\n      .setStyle(TextInputStyle.Short)\n      .setRequired(true)\n      .setMaxLength(1);\n\n    modal.addComponents(new ActionRowBuilder().addComponents(input));\n    return interaction.showModal(modal);\n  }\n\n  if (action === 'prefix') {\n    const modal = new ModalBuilder()\n      .setCustomId(`profile:set_prefix:${userId}`)\n      .setTitle('Editar prefixo');\n\n    const input = new TextInputBuilder()\n      .setCustomId('prefix')\n      .setLabel('Prefixo dos comandos')\n      .setPlaceholder('Exemplo: . ou ! ou ? ou ,')\n      .setStyle(TextInputStyle.Short)\n      .setRequired(true)\n      .setMaxLength(5);\n\n    modal.addComponents(new ActionRowBuilder().addComponents(input));\n    return interaction.showModal(modal);\n  }\n\n  if (action === 'reset') {\n    db.updateProfile(interaction.guildId, interaction.user.id, { fixedPassword: '', goTime: null, commandPrefix: '.' });\n    const userData = db.getUser(interaction.guildId, interaction.user.id);\n    return interaction.update({\n      embeds: [buildProfileEmbed(interaction.user, userData)],\n      components: buildProfileButtons(interaction.user.id)\n    });\n  }\n}\n\nasync function handleProfileModal(interaction, action, userId) {\n  if (interaction.user.id !== userId) {\n    return interaction.reply({ content: '❌ Esse perfil não é seu.', ephemeral: true });\n  }\n\n  if (action === 'set_password') {\n    const password = interaction.fields.getTextInputValue('password').trim();\n    if (!/^\\d+$/.test(password)) {\n      return interaction.reply({ content: '❌ A senha precisa ter apenas números.', ephemeral: true });\n    }\n\n    db.updateProfile(interaction.guildId, interaction.user.id, { fixedPassword: password });\n    const userData = db.getUser(interaction.guildId, interaction.user.id);\n    return interaction.reply({\n      content: '✅ Senha fixa salva.',\n      embeds: [buildProfileEmbed(interaction.user, userData)],\n      components: buildProfileButtons(interaction.user.id),\n      ephemeral: true\n    });\n  }\n\n  if (action === 'set_go') {\n    const go = Number(onlyNumbers(interaction.fields.getTextInputValue('go')));\n    if (!Number.isInteger(go) || go < 1 || go > 8) {\n      return interaction.reply({ content: '❌ O tempo de GO precisa ser de 1 a 8 minutos.', ephemeral: true });\n    }\n\n    db.updateProfile(interaction.guildId, interaction.user.id, { goTime: go });\n    const userData = db.getUser(interaction.guildId, interaction.user.id);\n    return interaction.reply({\n      content: '✅ Tempo de GO salvo.',\n      embeds: [buildProfileEmbed(interaction.user, userData)],\n      components: buildProfileButtons(interaction.user.id),\n      ephemeral: true\n    });\n  }\n\n  if (action === 'set_prefix') {\n    const prefix = interaction.fields.getTextInputValue('prefix').trim();\n    if (!prefix || /\\s/.test(prefix) || prefix.length > 5) {\n      return interaction.reply({ content: '❌ Prefixo inválido. Use de 1 a 5 caracteres e sem espaço.', ephemeral: true });\n    }\n\n    db.updateProfile(interaction.guildId, interaction.user.id, { commandPrefix: prefix });\n    const userData = db.getUser(interaction.guildId, interaction.user.id);\n    return interaction.reply({\n      content: `✅ Prefixo salvo: **${prefix}**`,\n      embeds: [buildProfileEmbed(interaction.user, userData)],\n      components: buildProfileButtons(interaction.user.id),\n      ephemeral: true\n    });\n  }\n}\n\nasync function handleOrderButton(interaction, action, orderId) {\n  const settings = db.getSettings(interaction.guildId);\n  const order = db.getOrder(interaction.guildId, orderId);\n\n  if (!order) {\n    return interaction.reply({ content: '❌ Pedido não encontrado.', ephemeral: true });\n  }\n\n  const isBuyer = interaction.user.id === order.userId;\n  const staff = isOwnerStaff(interaction.member, settings);\n\n  if (action === 'confirm') {\n    if (!staff) {\n      return interaction.reply({ content: '❌ Só dono/staff pode confirmar pagamento.', ephemeral: true });\n    }\n\n    if (order.status !== 'pending') {\n      return interaction.reply({ content: `❌ Esse pedido já está como **${order.status}**.`, ephemeral: true });\n    }\n\n    const userData = db.addBalance(interaction.guildId, order.userId, order.quantity, { countAsPurchase: true });\n    const updated = db.updateOrder(interaction.guildId, orderId, {\n      status: 'paid',\n      confirmedBy: interaction.user.id,\n      paidAt: new Date().toISOString()\n    });\n\n    await interaction.update({\n      embeds: [buildOrderEmbed(updated)],\n      components: buildOrderButtons(orderId, true)\n    });\n\n    await interaction.followUp({\n      content: `✅ Pagamento confirmado. <@${order.userId}> recebeu **${order.quantity} sala(s)**. Saldo atual: **${userData.balance} sala(s)**.`\n    });\n\n    await sendLog(interaction.guild, settings, {\n      embeds: [\n        new EmbedBuilder()\n          .setTitle('✅ Pagamento confirmado')\n          .setColor('#2ecc71')\n          .setDescription([\n            `Cliente: <@${order.userId}>`,\n            `Confirmado por: <@${interaction.user.id}>`,\n            `Pedido: **${order.id}**`,\n            `Quantidade: **${order.quantity} sala(s)**`,\n            `Valor: **${formatBRLFromCents(order.totalCents)}**`,\n            `Saldo atual: **${userData.balance} sala(s)**`\n          ].join('\\n'))\n          .setTimestamp()\n      ]\n    });\n\n    return;\n  }\n\n  if (action === 'cancel') {\n    if (!staff && !isBuyer) {\n      return interaction.reply({ content: '❌ Você não pode cancelar esse pedido.', ephemeral: true });\n    }\n\n    if (order.status !== 'pending') {\n      return interaction.reply({ content: '❌ Só pedidos aguardando pagamento podem ser cancelados.', ephemeral: true });\n    }\n\n    const updated = db.updateOrder(interaction.guildId, orderId, {\n      status: 'canceled',\n      canceledBy: interaction.user.id,\n      canceledAt: new Date().toISOString()\n    });\n\n    await interaction.update({\n      embeds: [buildOrderEmbed(updated)],\n      components: buildOrderButtons(orderId, true)\n    });\n\n    await sendLog(interaction.guild, settings, {\n      embeds: [\n        new EmbedBuilder()\n          .setTitle('❌ Pedido cancelado')\n          .setColor('#e74c3c')\n          .setDescription([\n            `Cliente: <@${order.userId}>`,\n            `Cancelado por: <@${interaction.user.id}>`,\n            `Pedido: **${order.id}**`,\n            `Quantidade: **${order.quantity} sala(s)**`\n          ].join('\\n'))\n          .setTimestamp()\n      ]\n    });\n\n    return;\n  }\n\n  if (action === 'close') {\n    if (!staff && !isBuyer) {\n      return interaction.reply({ content: '❌ Você não pode fechar esse canal.', ephemeral: true });\n    }\n\n    db.updateOrder(interaction.guildId, orderId, { status: order.status === 'pending' ? 'closed' : order.status });\n    await interaction.reply('🔒 Canal será fechado em 5 segundos...');\n    setTimeout(() => interaction.channel.delete('Canal de pedido fechado').catch(() => null), 5000);\n  }\n}\n\nmodule.exports = async function interactionCreate(interaction) {\n  if (!interaction.guild) return;\n\n  if (interaction.isChatInputCommand()) return handleCommand(interaction);\n\n  if (interaction.isButton()) {\n    const [scope, action, id, extra] = interaction.customId.split(':');\n\n    if (scope === 'sales' && action === 'buy') return openBuyModal(interaction);\n    if (scope === 'sales' && action === 'balance') return handleSalesBalance(interaction);\n    if (scope === 'sales' && action === 'create') return openCreateRoomModal(interaction);\n\n    if (scope === 'profile') return handleProfileButton(interaction, action, id);\n    if (scope === 'order') return handleOrderButton(interaction, action, id);\n    if (scope === 'roominfo' && action === 'refresh') return handleRoomInfoRefresh(interaction, id, extra);\n  }\n\n  if (interaction.isStringSelectMenu()) {\n    const [scope, action, id] = interaction.customId.split(':');\n    if (scope === 'roommode' && action === 'select') return handleRoomModeSelect(interaction, id);\n  }\n\n  if (interaction.isModalSubmit()) {\n    const [scope, action, id] = interaction.customId.split(':');\n\n    if (scope === 'order' && action === 'create') return handleCreateOrder(interaction);\n    if (scope === 'room' && action === 'create') return handleCreateRoomModal(interaction);\n    if (scope === 'profile') return handleProfileModal(interaction, action, id);\n  }\n};\n", "src/handlers/messageCreate.js": "const db = require('../database');\nconst { buildRoomModePanel } = require('../utils/embeds');\nconst { getRoomModeOption } = require('../constants');\nconst { createRoomForMessage } = require('./roomCreate');\n\nfunction escapeRegex(value) {\n  return String(value).replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');\n}\n\nmodule.exports = async function messageCreate(message) {\n  if (!message.guild || message.author.bot) return;\n\n  const userData = db.getUser(message.guild.id, message.author.id);\n  const prefix = userData.profile?.commandPrefix || '.';\n  const content = message.content.trim();\n\n  if (!content.startsWith(prefix)) return;\n\n  const body = content.slice(prefix.length).trim().toLowerCase();\n  const match = body.match(/^(cr|cs)\\s*(\\d+)?$/i);\n  if (!match) return;\n\n  const modeNumber = match[2];\n\n  if (!modeNumber) {\n    return message.reply(buildRoomModePanel(message.author, prefix, false));\n  }\n\n  const selected = getRoomModeOption(modeNumber);\n  if (!selected) {\n    return message.reply(`❌ Modo inválido. Use **${prefix}cr**, **${prefix}cr 1**, **${prefix}cr 2**, **${prefix}cr 3**, **${prefix}cr 4** ou **${prefix}cr 5**.`);\n  }\n\n  return createRoomForMessage(message, {\n    modo: selected.apiMode,\n    modeOption: selected.value,\n    nome: `Sala ${message.author.username}`\n  });\n};\n", "src/handlers/roomCreate.js": "const db = require('../database');\nconst salasApi = require('../services/salasApi');\nconst { ROOM_MODES, getRoomModeOption } = require('../constants');\nconst { onlyNumbers } = require('../utils/format');\nconst { buildRoomCreatedEmbed, buildRoomInfoButtons } = require('../utils/embeds');\n\nasync function sendLogFromInteraction(interaction, settings, embed) {\n  if (!settings.logsChannelId) return;\n  const channel = await interaction.guild.channels.fetch(settings.logsChannelId).catch(() => null);\n  if (channel?.isTextBased()) {\n    await channel.send({ embeds: [embed] }).catch(() => null);\n  }\n}\n\nasync function sendLogFromMessage(message, settings, embed) {\n  if (!settings.logsChannelId) return;\n  const channel = await message.guild.channels.fetch(settings.logsChannelId).catch(() => null);\n  if (channel?.isTextBased()) {\n    await channel.send({ embeds: [embed] }).catch(() => null);\n  }\n}\n\nfunction normalizePayload(payload, username) {\n  const selected = getRoomModeOption(payload.modeOption || payload.modo);\n  const apiMode = selected?.apiMode || Number(payload.modo);\n\n  return {\n    modo: apiMode,\n    modeOption: selected?.value || String(payload.modeOption || payload.modo || ''),\n    modeLabel: selected?.name || ROOM_MODES[apiMode] || String(apiMode),\n    nome: payload.nome || `Sala ${username}`,\n    senha: payload.senha || '',\n    tempoGo: payload.tempoGo || null\n  };\n}\n\nasync function createRoomCore({ guildId, user, memberLabel, guild, payload, reply, sendLog }) {\n  const settings = db.getSettings(guildId);\n  const userData = db.getUser(guildId, user.id);\n\n  if (userData.balance < 1) {\n    return reply('❌ Você não possui saldo! Compre salas no painel primeiro.');\n  }\n\n  const profile = userData.profile || {};\n  const finalPayload = normalizePayload(payload, user.username);\n  const modo = Number(finalPayload.modo);\n  const nome = finalPayload.nome;\n  const senhaFromInput = finalPayload.senha ? onlyNumbers(finalPayload.senha) : '';\n  const senha = senhaFromInput || profile.fixedPassword || '';\n  const autoStart = Number(finalPayload.tempoGo || profile.goTime || 0) || null;\n\n  if (!ROOM_MODES[modo]) {\n    return reply('❌ Modo inválido. Use um modo válido.');\n  }\n\n  if (finalPayload.senha && senhaFromInput !== String(finalPayload.senha).trim()) {\n    return reply('❌ A senha precisa ter apenas números.');\n  }\n\n  if (senha && !/^\\d+$/.test(senha)) {\n    return reply('❌ Sua senha fixa do /perfil está inválida. Use apenas números.');\n  }\n\n  if (autoStart && (autoStart < 1 || autoStart > 8)) {\n    return reply('❌ O tempo de GO precisa ser de 1 a 8 minutos.');\n  }\n\n  try {\n    const data = await salasApi.criarSala({ modo, senha, nome, autoStart });\n    const result = db.removeBalance(guildId, user.id, 1);\n\n    if (!result.ok) {\n      return reply('❌ Seu saldo acabou antes de descontar. Tente novamente.');\n    }\n\n    let info = null;\n    if (data.sshash) {\n      info = await salasApi.infoSala(data.sshash).catch(() => null);\n    }\n\n    db.addRoomLog(guildId, user.id, {\n      roomId: data.roomId || '',\n      sshash: data.sshash || '',\n      senha: data.senha || senha || '',\n      nome: data.nome || nome,\n      modo,\n      modeOption: finalPayload.modeOption,\n      modoCriado: finalPayload.modeLabel || data.modoCriado || ROOM_MODES[modo],\n      autoStart: autoStart || null\n    });\n\n    const embed = buildRoomCreatedEmbed(memberLabel, data, result.user.balance, {\n      modo,\n      modeOption: finalPayload.modeOption,\n      modeLabel: finalPayload.modeLabel,\n      senha,\n      nome,\n      autoStart\n    }, info);\n\n    const components = buildRoomInfoButtons(data.sshash, user.id);\n    await sendLog(settings, embed);\n    return reply({ embeds: [embed], components });\n  } catch (error) {\n    console.error('Erro criando sala:', error);\n    return reply(`❌ Erro ao criar sala: ${error.message}`);\n  }\n}\n\nasync function createRoomForInteraction(interaction, payload, options = {}) {\n  const reply = async (contentOrPayload) => {\n    const normalized = typeof contentOrPayload === 'string' ? { content: contentOrPayload } : contentOrPayload;\n    if (options.alreadyDeferred) return interaction.editReply(normalized);\n    if (interaction.deferred || interaction.replied) return interaction.editReply(normalized);\n    return interaction.reply({ ...normalized, ephemeral: options.ephemeral !== false });\n  };\n\n  return createRoomCore({\n    guildId: interaction.guildId,\n    user: interaction.user,\n    memberLabel: interaction.user,\n    guild: interaction.guild,\n    payload,\n    reply,\n    sendLog: (settings, embed) => sendLogFromInteraction(interaction, settings, embed)\n  });\n}\n\nasync function createRoomForMessage(message, payload) {\n  const waiting = await message.reply('⏳ Criando sua sala...').catch(() => null);\n\n  const reply = async (contentOrPayload) => {\n    const normalized = typeof contentOrPayload === 'string' ? { content: contentOrPayload } : contentOrPayload;\n    if (waiting) return waiting.edit(normalized).catch(() => message.reply(normalized));\n    return message.reply(normalized);\n  };\n\n  return createRoomCore({\n    guildId: message.guild.id,\n    user: message.author,\n    memberLabel: message.author,\n    guild: message.guild,\n    payload,\n    reply,\n    sendLog: (settings, embed) => sendLogFromMessage(message, settings, embed)\n  });\n}\n\nmodule.exports = {\n  createRoomForInteraction,\n  createRoomForMessage\n};\n", "src/services/salasApi.js": "const BASE_URL = process.env.BASE_URL || 'https://salas-bot.freefireapi.online';\n\nfunction getApiKey() {\n  const key = process.env.SALAS_API_KEY;\n  if (!key || key === 'COLOQUE_SUA_KEY_DA_API_AQUI') {\n    throw new Error('SALAS_API_KEY não configurada no .env');\n  }\n  return key;\n}\n\nasync function requestApi(endpoint, body = {}) {\n  const controller = new AbortController();\n  const timeout = setTimeout(() => controller.abort(), 20000);\n\n  try {\n    const response = await fetch(`${BASE_URL}${endpoint}`, {\n      method: 'POST',\n      headers: {\n        Authorization: getApiKey(),\n        'Content-Type': 'application/json'\n      },\n      body: JSON.stringify(body),\n      signal: controller.signal\n    });\n\n    let data = null;\n    try {\n      data = await response.json();\n    } catch (_) {\n      data = { msg: await response.text().catch(() => '') };\n    }\n\n    if (!response.ok) {\n      const code = data?.status || data?.error || response.status;\n      const message = data?.msg || data?.message || 'Erro desconhecido na API';\n      throw new Error(`${code}: ${message}`);\n    }\n\n    return data;\n  } catch (error) {\n    if (error.name === 'AbortError') {\n      throw new Error('A API demorou muito para responder. Tente novamente.');\n    }\n    throw error;\n  } finally {\n    clearTimeout(timeout);\n  }\n}\n\nasync function verificarKey() {\n  return requestApi('/api/verificar-key');\n}\n\nasync function statusSistema() {\n  return requestApi('/api/status');\n}\n\nasync function criarSala({ modo, senha, nome, autoStart }) {\n  const payload = {\n    modo: Number(modo),\n    senha: senha || undefined,\n    nome: nome || undefined,\n    auto_start: autoStart ? Number(autoStart) : undefined\n  };\n\n  Object.keys(payload).forEach((key) => payload[key] === undefined && delete payload[key]);\n  return requestApi('/api/v1/create:room', payload);\n}\n\nasync function iniciarSala(sshash, enviarMsg = true) {\n  return requestApi('/api/v2/start:room', { sshash, enviar_msg: enviarMsg });\n}\n\nasync function infoSala(sshash) {\n  return requestApi('/api/v2/info:room', { sshash });\n}\n\nmodule.exports = {\n  verificarKey,\n  statusSistema,\n  criarSala,\n  iniciarSala,\n  infoSala\n};\n", "src/utils/embeds.js": "const {\n  ActionRowBuilder,\n  ButtonBuilder,\n  ButtonStyle,\n  EmbedBuilder,\n  StringSelectMenuBuilder,\n  StringSelectMenuOptionBuilder\n} = require('discord.js');\nconst { BUTTON_STYLES, ROOM_MODES, ROOM_MODE_OPTIONS } = require('../constants');\nconst { formatBRLFromCents, statusLabel } = require('./format');\n\nfunction buildSalesPanel(settings) {\n  const embed = new EmbedBuilder()\n    .setTitle(settings.panel.title)\n    .setDescription(settings.panel.description)\n    .setColor(settings.panel.color || '#2b2d31')\n    .setFooter({ text: settings.panel.footer || 'Sistema de vendas por saldo' });\n\n  if (settings.panel.imageUrl) embed.setImage(settings.panel.imageUrl);\n\n  const buy = settings.buttons.buy;\n  const balance = settings.buttons.balance;\n  const create = settings.buttons.create;\n\n  const row = new ActionRowBuilder().addComponents(\n    new ButtonBuilder()\n      .setCustomId('sales:buy')\n      .setLabel(buy.label || 'Comprar salas')\n      .setEmoji(buy.emoji || '🛒')\n      .setStyle(BUTTON_STYLES[buy.style] || ButtonStyle.Success),\n    new ButtonBuilder()\n      .setCustomId('sales:balance')\n      .setLabel(balance.label || 'Ver saldo')\n      .setEmoji(balance.emoji || '💰')\n      .setStyle(BUTTON_STYLES[balance.style] || ButtonStyle.Primary),\n    new ButtonBuilder()\n      .setCustomId('sales:create')\n      .setLabel(create.label || 'Criar sala')\n      .setEmoji(create.emoji || '🎮')\n      .setStyle(BUTTON_STYLES[create.style] || ButtonStyle.Secondary)\n  );\n\n  return { embeds: [embed], components: [row] };\n}\n\nfunction buildOrderEmbed(order, user) {\n  return new EmbedBuilder()\n    .setTitle('🛒 Pedido de salas')\n    .setColor(order.status === 'paid' ? '#2ecc71' : order.status === 'canceled' ? '#e74c3c' : '#f1c40f')\n    .setDescription([\n      `Cliente: <@${order.userId}>`,\n      `Quantidade: **${order.quantity} sala(s)**`,\n      `Valor: **${formatBRLFromCents(order.totalCents)}**`,\n      `Status: **${statusLabel(order.status)}**`,\n      order.confirmedBy ? `Confirmado por: <@${order.confirmedBy}>` : null,\n      '',\n      order.status === 'pending' ? 'Depois que o pagamento cair, o dono/staff clica em **Confirmar pagamento**.' : null\n    ].filter(Boolean).join('\\n'))\n    .setFooter({ text: `Pedido ${order.id}${user?.username ? ` • ${user.username}` : ''}` })\n    .setTimestamp(new Date(order.updatedAt || order.createdAt || Date.now()));\n}\n\nfunction buildOrderButtons(orderId, disabled = false) {\n  return [\n    new ActionRowBuilder().addComponents(\n      new ButtonBuilder()\n        .setCustomId(`order:confirm:${orderId}`)\n        .setLabel('Confirmar pagamento')\n        .setEmoji('✅')\n        .setStyle(ButtonStyle.Success)\n        .setDisabled(disabled),\n      new ButtonBuilder()\n        .setCustomId(`order:cancel:${orderId}`)\n        .setLabel('Cancelar pedido')\n        .setEmoji('❌')\n        .setStyle(ButtonStyle.Danger)\n        .setDisabled(disabled),\n      new ButtonBuilder()\n        .setCustomId(`order:close:${orderId}`)\n        .setLabel('Fechar canal')\n        .setEmoji('🔒')\n        .setStyle(ButtonStyle.Secondary)\n    )\n  ];\n}\n\nfunction buildBalanceEmbed(member, userData) {\n  return new EmbedBuilder()\n    .setTitle('💰 Saldo')\n    .setColor('#2b2d31')\n    .setDescription([\n      `Cliente: ${member}`,\n      `Saldo atual: **${userData.balance} sala(s)**`,\n      `Salas compradas: **${userData.stats?.totalBought || 0}**`,\n      `Salas criadas: **${userData.stats?.roomsCreated || 0}**`\n    ].join('\\n'));\n}\n\nfunction buildProfileEmbed(member, userData) {\n  const profile = userData.profile || {};\n  return new EmbedBuilder()\n    .setTitle('👤 Perfil de criação de sala')\n    .setColor('#2b2d31')\n    .setDescription([\n      `Cliente: ${member}`,\n      `Senha fixa: **${profile.fixedPassword || 'Não configurada'}**`,\n      `Tempo de GO: **${profile.goTime ? `${profile.goTime} minuto(s)` : 'Não configurado'}**`,\n      `Prefixo: **${profile.commandPrefix || '.'}**`,\n      '',\n      `Com seu prefixo você pode usar: **${profile.commandPrefix || '.'}cr**, **${profile.commandPrefix || '.'}cs**, **${profile.commandPrefix || '.'}cr 1** ou **${profile.commandPrefix || '.'}cr1**.`,\n      'Essas configurações são só suas. Quando você criar sala, o bot usa sua senha, seu tempo de GO e seu prefixo.'\n    ].join('\\n'));\n}\n\nfunction buildProfileButtons(userId) {\n  return [\n    new ActionRowBuilder().addComponents(\n      new ButtonBuilder()\n        .setCustomId(`profile:password:${userId}`)\n        .setLabel('Editar senha fixa')\n        .setEmoji('🔐')\n        .setStyle(ButtonStyle.Primary),\n      new ButtonBuilder()\n        .setCustomId(`profile:go:${userId}`)\n        .setLabel('Editar tempo de GO')\n        .setEmoji('⏱️')\n        .setStyle(ButtonStyle.Success),\n      new ButtonBuilder()\n        .setCustomId(`profile:prefix:${userId}`)\n        .setLabel('Editar prefixo')\n        .setEmoji('⌨️')\n        .setStyle(ButtonStyle.Secondary),\n      new ButtonBuilder()\n        .setCustomId(`profile:reset:${userId}`)\n        .setLabel('Resetar')\n        .setEmoji('🗑️')\n        .setStyle(ButtonStyle.Danger)\n    )\n  ];\n}\n\nfunction buildRoomModePanel(user, prefix = '.', ephemeralNote = false) {\n  const embed = new EmbedBuilder()\n    .setTitle('🎮 Criar Sala')\n    .setColor('#5865f2')\n    .setDescription([\n      'Escolha o modo da sala que deseja criar:',\n      '',\n      ROOM_MODE_OPTIONS.map((mode) => `${mode.emoji} **${mode.name}** — ${mode.description}`).join('\\n'),\n      '',\n      `Atalho: **${prefix}cr 1**, **${prefix}cr 2**, **${prefix}cr 3**...`,\n      ephemeralNote ? 'Esse menu só funciona para você.' : null\n    ].filter(Boolean).join('\\n'))\n    .setFooter({ text: 'FF Salas Bot' });\n\n  const select = new StringSelectMenuBuilder()\n    .setCustomId(`roommode:select:${user.id}`)\n    .setPlaceholder('Escolha o modo da sala...')\n    .addOptions(\n      ROOM_MODE_OPTIONS.map((mode) =>\n        new StringSelectMenuOptionBuilder()\n          .setLabel(mode.name)\n          .setDescription(mode.description)\n          .setValue(mode.value)\n          .setEmoji(mode.emoji)\n      )\n    );\n\n  return { embeds: [embed], components: [new ActionRowBuilder().addComponents(select)] };\n}\n\nfunction formatPlayers(group) {\n  if (!group || !Array.isArray(group.players) || group.players.length === 0) {\n    return 'Aguardando jogadores...';\n  }\n\n  return group.players\n    .map((player, index) => {\n      const ready = player.ready ? '✅' : '⏳';\n      const platform = player.playerPlatform ? ` • ${player.playerPlatform}` : '';\n      const id = player.playerID ? ` \\`${player.playerID}\\`` : '';\n      return `${index + 1}. ${ready} **${player.nickname || 'Jogador'}**${id}${platform}`;\n    })\n    .join('\\n');\n}\n\nfunction normalizeRoomInfo(info) {\n  const groups = Array.isArray(info?.infoGroups) ? info.infoGroups : [];\n  const team1 = groups.find((group) => Number(group.teamIndex) === 1) || groups[0] || null;\n  const team2 = groups.find((group) => Number(group.teamIndex) === 2) || groups[1] || null;\n\n  return {\n    totalPlayers: Number(info?.totalPlayers || 0),\n    team1,\n    team2,\n    state: info?.room?.estado || info?.status_atual || 'Aguardando jogadores'\n  };\n}\n\nfunction buildRoomCreatedEmbed(member, data, balance, finalConfig, info = null) {\n  const roomInfo = normalizeRoomInfo(info);\n  const modeLabel = finalConfig.modeLabel || data.modoCriado || ROOM_MODES[finalConfig.modo] || finalConfig.modo;\n\n  const embed = new EmbedBuilder()\n    .setTitle('⚡ Nova Sala Criada')\n    .setColor('#2ecc71')\n    .setDescription([\n      `👤 Cliente: ${member}`,\n      `🏠 Servidor/Nome: **${data.nome || finalConfig.nome || 'Sala'}**`,\n      `🆔 ID da sala: **${data.roomId || 'Não informado'}**`,\n      `🔐 Senha: **${data.senha || finalConfig.senha || 'Sem senha'}**`,\n      `🎮 Modo: **${modeLabel}**`,\n      finalConfig.autoStart ? `⏱️ GO: **${finalConfig.autoStart} minuto(s)**` : '⏱️ GO: **Manual**',\n      `💰 Saldo restante: **${balance} sala(s)**`,\n      '',\n      `📊 Status: **${roomInfo.state}**`,\n      `👥 Jogadores: **${roomInfo.totalPlayers || 0}**`\n    ].join('\\n'))\n    .addFields(\n      { name: roomInfo.team1?.teamLabel || 'Time 1', value: formatPlayers(roomInfo.team1), inline: false },\n      { name: roomInfo.team2?.teamLabel || 'Time 2', value: formatPlayers(roomInfo.team2), inline: false }\n    )\n    .setFooter({ text: data.sshash ? 'Use o botão Atualizar jogadores para ver quem entrou.' : 'Sala criada via API' })\n    .setTimestamp();\n\n  return embed;\n}\n\nfunction buildRoomInfoButtons(sshash, userId) {\n  if (!sshash) return [];\n\n  return [\n    new ActionRowBuilder().addComponents(\n      new ButtonBuilder()\n        .setCustomId(`roominfo:refresh:${userId}:${sshash}`)\n        .setLabel('Atualizar jogadores')\n        .setEmoji('🔄')\n        .setStyle(ButtonStyle.Primary)\n    )\n  ];\n}\n\nmodule.exports = {\n  buildSalesPanel,\n  buildOrderEmbed,\n  buildOrderButtons,\n  buildBalanceEmbed,\n  buildProfileEmbed,\n  buildProfileButtons,\n  buildRoomModePanel,\n  buildRoomCreatedEmbed,\n  buildRoomInfoButtons\n};\n", "src/utils/format.js": "function formatBRLFromCents(cents) {\n  return (Number(cents) / 100).toLocaleString('pt-BR', {\n    style: 'currency',\n    currency: 'BRL'\n  });\n}\n\nfunction onlyNumbers(value) {\n  return String(value || '').replace(/\\D/g, '');\n}\n\nfunction parseHexColor(value) {\n  if (!value) return null;\n  const color = String(value).trim();\n  if (/^#[0-9a-fA-F]{6}$/.test(color)) return color;\n  if (/^[0-9a-fA-F]{6}$/.test(color)) return `#${color}`;\n  return null;\n}\n\nfunction cleanChannelName(name) {\n  return String(name || 'cliente')\n    .normalize('NFD')\n    .replace(/[\\u0300-\\u036f]/g, '')\n    .toLowerCase()\n    .replace(/[^a-z0-9-]/g, '-')\n    .replace(/-+/g, '-')\n    .slice(0, 35)\n    .replace(/^-|-$/g, '') || 'cliente';\n}\n\nfunction statusLabel(status) {\n  const map = {\n    pending: 'Aguardando pagamento',\n    paid: 'Pagamento confirmado',\n    canceled: 'Cancelado',\n    closed: 'Fechado'\n  };\n  return map[status] || status;\n}\n\nmodule.exports = {\n  formatBRLFromCents,\n  onlyNumbers,\n  parseHexColor,\n  cleanChannelName,\n  statusLabel\n};\n", "src/utils/permissions.js": "const { PermissionFlagsBits } = require('discord.js');\n\nfunction isAdmin(member) {\n  return Boolean(member?.permissions?.has(PermissionFlagsBits.Administrator));\n}\n\nfunction isOwnerStaff(member, settings) {\n  if (!member) return false;\n  if (isAdmin(member)) return true;\n  if (!settings?.ownerRoleId) return false;\n  return member.roles?.cache?.has(settings.ownerRoleId) || false;\n}\n\nmodule.exports = {\n  isAdmin,\n  isOwnerStaff\n};\n"};
const __moduleCache = {};

function __resolveLocal(fromId, request) {
  if (!request.startsWith('.')) return null;
  const base = path.posix.dirname(fromId);
  let target = path.posix.normalize(path.posix.join(base, request));
  if (__moduleSources[target]) return target;
  if (__moduleSources[`${target}.js`]) return `${target}.js`;
  if (__moduleSources[`${target}/index.js`]) return `${target}/index.js`;
  throw new Error(`Módulo local não encontrado: ${request} chamado por ${fromId}`);
}

function __localRequire(fromId, request) {
  const resolved = __resolveLocal(fromId, request);
  if (!resolved) return require(request);
  if (__moduleCache[resolved]) return __moduleCache[resolved].exports;

  const module = { exports: {} };
  __moduleCache[resolved] = module;
  const filename = path.join(process.cwd(), resolved);
  const dirname = path.dirname(filename);
  const fn = new Function('require', 'module', 'exports', '__filename', '__dirname', __moduleSources[resolved]);
  fn((req) => __localRequire(resolved, req), module, module.exports, filename, dirname);
  return module.exports;
}

function cleanEnv(value, name) {
  let result = String(value || '').trim();
  if (result.startsWith(`${name}=`)) result = result.slice(name.length + 1).trim();
  if ((result.startsWith('\"') && result.endsWith('\"')) || (result.startsWith("'") && result.endsWith("'"))) {
    result = result.slice(1, -1).trim();
  }
  return result;
}

const token = cleanEnv(process.env.DISCORD_TOKEN, 'DISCORD_TOKEN');
const clientIdFromEnv = cleanEnv(process.env.CLIENT_ID, 'CLIENT_ID');
const guildId = cleanEnv(process.env.GUILD_ID, 'GUILD_ID');

if (!token || token === 'COLOQUE_SEU_TOKEN_AQUI' || token === 'SEU_TOKEN_DO_BOT') {
  console.error('❌ DISCORD_TOKEN não configurado nas Variables do Railway');
  process.exit(1);
}

const client = new Client({
  intents: [
    GatewayIntentBits.Guilds,
    GatewayIntentBits.GuildMessages,
    GatewayIntentBits.MessageContent
  ]
});

client.commands = new Collection();

const commandIds = ["src/commands/config-vendas.js", "src/commands/criar-sala.js", "src/commands/painel-vendas.js", "src/commands/perfil.js", "src/commands/saldo.js"];

const commandsJson = [];
for (const id of commandIds) {
  const command = __localRequire('index.js', `./${id}`);
  if (!command.data || !command.execute) {
    console.warn(`⚠️ Comando ignorado: ${id}`);
    continue;
  }
  client.commands.set(command.data.name, command);
  commandsJson.push(command.data.toJSON());
}

async function registerCommands() {
  const rest = new REST({ version: '10' }).setToken(token);
  const applicationId = client.application?.id || clientIdFromEnv;

  if (!applicationId) {
    throw new Error('Não consegui pegar o ID do aplicativo. Confira o token do bot.');
  }

  const configuredGuildId = guildId && guildId !== 'COLOQUE_O_ID_DO_SEU_SERVIDOR_AQUI' && guildId !== 'ID_DO_SEU_SERVIDOR' ? guildId : null;

  console.log(`📌 Registrando comandos usando Application ID: ${applicationId}`);

  if (configuredGuildId) {
    await rest.put(Routes.applicationGuildCommands(applicationId, configuredGuildId), { body: commandsJson });
    console.log(`✅ ${commandsJson.length} comandos registrados no servidor ${configuredGuildId}.`);
  } else {
    await rest.put(Routes.applicationCommands(applicationId), { body: commandsJson });
    console.log(`✅ ${commandsJson.length} comandos globais registrados. Pode demorar para aparecer.`);
  }
}

client.once('ready', async () => {
  console.log(`🤖 Bot online como ${client.user.tag}`);
  try {
    await registerCommands();
  } catch (error) {
    console.error('❌ Erro registrando comandos:', error);
  }
});

client.on('interactionCreate', __localRequire('index.js', './src/handlers/interactionCreate.js'));
client.on('messageCreate', __localRequire('index.js', './src/handlers/messageCreate.js'));

process.on('unhandledRejection', (reason) => console.error('Unhandled Rejection:', reason));
process.on('uncaughtException', (error) => console.error('Uncaught Exception:', error));

client.login(token);
